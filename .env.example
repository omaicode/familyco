# Runtime environment
NODE_ENV=development

# Database
DB_PROVIDER=sqlite
DATABASE_URL=file:./dev.db

# Server
PORT=4000
API_PREFIX=/api/v1
FAMILYCO_REPOSITORY_DRIVER=memory

# Auth placeholders
JWT_SECRET=replace-with-local-dev-secret
API_KEY_SALT=replace-with-local-dev-salt
