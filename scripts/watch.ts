const startedAt = new Date().toISOString();

console.log(`[familyco] workspace watch placeholder started at ${startedAt}`);
console.log('[familyco] Use "pnpm dev:p" to run package-level dev watchers in parallel.');
