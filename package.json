{
  "name": "familyco",
  "version": "1.0.0",
  "description": "FamilyCo monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:electron": "pnpm build && pnpm --filter @familyco/electron run build:electron",
    "dev": "turbo run dev --parallel --filter=@familyco/server --filter=@familyco/web",
    "dev:electron": "pnpm --filter './packages/**' build && pnpm --filter @familyco/server build && turbo run dev --parallel --filter=@familyco/web --filter=@familyco/electron",
    "dev:p": "turbo run dev --parallel",
    "prisma:generate": "pnpm --filter @familyco/db run prisma:generate",
    "prisma:migrate:dev": "pnpm --filter @familyco/db run prisma:migrate:dev",
    "prisma:migrate": "pnpm --filter @familyco/db run prisma:migrate",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "keywords": [
    "familyco",
    "ai",
    "monorepo"
  ],
  "author": "",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "axios": "^1.14.0",
    "dotenv": "^17.4.0",
    "tslib": "^2.8.1",
    "tsx": "^4.21.0",
    "turbo": "^2.9.3",
    "vite": "^8.0.3",
    "vite-plugin-dts": "^4.5.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^24.12.2"
  }
}